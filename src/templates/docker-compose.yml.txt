version: '3.8'
services:
  mendix:
    image: mendix/custom-app:latest
    ports:
      - "{{CLIENT_PORT}}:8080"
    environment:
      - DATABASE_HOST=postgres
      - DATABASE_PORT={{POSTGRES_PORT}}
    depends_on:
      - postgres

  postgres:
    image: postgres:13
    environment:
      - POSTGRES_USER=mx
      - POSTGRES_PASSWORD=mx
    ports:
      - "{{POSTGRES_PORT}}:5432"
